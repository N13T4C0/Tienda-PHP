# Activamos el motor de reescritura
RewriteEngine On

# Si la peticion NO es a un fichero o carpeta existente,
# la redirigimos al front controller (index.php)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php?ruta=$1 [QSA,L]
